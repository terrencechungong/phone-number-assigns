# Full origin of clickupbackend (no trailing slash). Leave empty in dev to use Vite proxy to click.acquisition-central.com.
VITE_FORMFLOW_CRM_API_BASE=

# Same key as Formflow Buddy (FORMFLOW_CRM_API_KEY on server).
VITE_FORMFLOW_CRM_API_KEY=
